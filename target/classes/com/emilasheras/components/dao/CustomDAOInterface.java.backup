package com.emilasheras.components.dao;

import com.emilasheras.components.models.CModel;

public interface CustomDAOInterface<T extends CModel> {
    void save(T model);
    void update(T model);
    void delete(T model);
}