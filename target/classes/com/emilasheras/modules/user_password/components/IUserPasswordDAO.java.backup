package com.emilasheras.modules.user.components;

import java.util.List;

import com.emilasheras.modules.user.models.UserPasswordBackup;

public interface IUserPasswordDAO {
    void save(UserPasswordBackup userPassword); // todo: update this to use the new CModel class
    UserPasswordBackup findByUsername(String username);
    List<UserPasswordBackup> findAll();
    void update(UserPasswordBackup userPassword); // todo: update this to use the new CModel class
    void delete(UserPasswordBackup userPassword); // todo: update this to use the new CModel class
}